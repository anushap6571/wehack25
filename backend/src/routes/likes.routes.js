/*const express = require('express');
const router = express.Router();
const likesController = require('../controllers/likes.controller');

// Route to toggle like status of a stock
router.post('/:symbol/toggle', likesController.toggleLike);

// Route to get all liked stocks
router.get('/', likesController.getLikedStocks);

module.exports = router; */